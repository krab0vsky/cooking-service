<h1 class="mb-4">Справочник категорий</h1>

<a href="/recipes" class="btn btn-secondary mb-3">← Назад</a>

<div class="row">
    <div class="col-md-8 mx-auto">

        {{!-- Сообщения об ошибках и успехе --}}
        {{#if error}}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                {{error}}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {{/if}}

        {{#if message}}
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i>
                {{message}}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {{/if}}

        <div class="card">
            <div class="card-header">
                <h3 class="mb-0">
                    <i class="bi bi-tags me-2"></i>Категории рецептов
                </h3>
            </div>
            <div class="card-body">

                {{!-- Форма добавления категории --}}
                <div class="mb-4">
                    <h5 class="mb-3">Добавить новую категорию</h5>
                    <form action="/admin/dicts/category/add" method="POST" class="row g-3">
                        <div class="col-md-8">
                            <input type="text" 
                                   name="name" 
                                   placeholder="Название категории" 
                                   class="form-control" 
                                   required
                                   maxlength="100">
                            <div class="form-text">Название должно быть уникальным</div>
                        </div>
                        <div class="col-md-4">
                            <button type="submit" class="btn btn-primary w-100">
                                <i class="bi bi-plus-circle me-2"></i>Добавить
                            </button>
                        </div>
                    </form>
                </div>

                <hr>

                {{!-- Список категорий --}}
                <h5 class="mb-3">Список категорий</h5>
                
                {{#if categories.length}}
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th width="50">ID</th>
                                    <th>Название категории</th>
                                    <th width="120">Действия</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each categories}}
                                    <tr>
                                        <td class="text-muted">#{{this.id}}</td>
                                        <td>
                                            <strong>{{this.name}}</strong>
                                            {{#if this.active}}
                                                <span class="badge bg-success ms-2">Активна</span>
                                            {{else}}
                                                <span class="badge bg-secondary ms-2">Неактивна</span>
                                            {{/if}}
                                        </td>
                                        <td>
                                            <a href="/admin/dicts/category/delete/{{this.id}}"
                                               class="btn btn-sm btn-outline-danger"
                                               onclick="return confirm('Удалить категорию «{{this.name}}»?')"
                                               title="Удалить категорию">
                                                <i class="bi bi-trash"></i>
                                            </a>
                                        </td>
                                    </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                    
                    <div class="text-muted small mt-3">
                        <i class="bi bi-info-circle me-1"></i>
                        Категорию можно удалить только если она не используется в рецептах
                    </div>
                {{else}}
                    <div class="text-center py-5">
                        <i class="bi bi-tags display-1 text-muted mb-3"></i>
                        <p class="text-muted">Категорий пока нет. Добавьте первую категорию.</p>
                    </div>
                {{/if}}

            </div>
        </div>

    </div>
</div>